import { About, Contact, Cover, Experience, Template } from '../components';

const Portfolio = () => {
  return (
    <Template>
      <Cover />
      <About />
      <Experience />
      <Contact />
    </Template>
  );
};

export default Portfolio;
