import { Template, Intro, Experience, Contact, Footer } from '../components';

const Portfolio = () => {
  return (
    <Template>
      <Intro />
      <Experience />
      <Contact />
      <Footer />
    </Template>
  );
};

export default Portfolio;
