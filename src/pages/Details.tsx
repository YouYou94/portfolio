import { Outlet } from 'react-router-dom';
import { Template } from '../components';

const Details = () => {
  return (
    <Template>
      <Outlet />
    </Template>
  );
};

export default Details;
