import { Cover, Menu, SingleTemplate } from '../components';

const HomeContainer = () => {
  return (
    <SingleTemplate>
      <Cover title="Portfolio" />
      <Menu />
    </SingleTemplate>
  );
};

export default HomeContainer;
