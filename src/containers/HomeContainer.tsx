import { Cover, Menu, SingleTemplate } from '../components';

const HomeContainer = () => {
  return (
    <SingleTemplate>
      <Cover />
      <Menu />
    </SingleTemplate>
  );
};

export default HomeContainer;
